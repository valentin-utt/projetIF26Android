<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph.xml"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="fr.utt.if26.projet.activity.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/mapsActivity"
        android:name="fr.utt.if26.projet.activity.MapsActivity"
        android:label="activity_maps"
        tools:layout="@layout/activity_maps" />
    <activity
        android:id="@+id/viewCacheActivity"
        android:name="fr.utt.if26.projet.activity.ViewCacheActivity"
        android:label="ViewCacheActivity" />
    <activity
        android:id="@+id/legalActivity"
        android:name="fr.utt.if26.projet.activity.LegalActivity"
        android:label="activity_legal"
        tools:layout="@layout/activity_legal" />
    <activity
        android:id="@+id/loginActivity"
        android:name="fr.utt.if26.projet.activity.LoginActivity"
        android:label="activity_login"
        tools:layout="@layout/activity_login" />
    <activity
        android:id="@+id/filterActivity"
        android:name="fr.utt.if26.projet.activity.FilterActivity"
        android:label="activity_filter"
        tools:layout="@layout/activity_filter" />
</navigation>